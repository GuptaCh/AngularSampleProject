import { CgDirective } from './cg.directive';

describe('CgDirective', () => {
  it('should create an instance', () => {
    const directive = new CgDirective();
    expect(directive).toBeTruthy();
  });
});
